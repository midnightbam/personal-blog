{
  "name": "personal-blog",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --cwd=client install --legacy-peer-deps && npm --cwd=client run build && mkdir -p dist && cp -r client/dist/* dist/",
    "dev": "npm --cwd=client run dev"
  }
}
