{{

  "buildCommand": "npm --prefix client install --legacy-peer-deps && npm --prefix client run build",  "outputDirectory": "dist"

  "outputDirectory": "client/dist",}
  "framework": "vite",
  "installCommand": "npm install"
}
